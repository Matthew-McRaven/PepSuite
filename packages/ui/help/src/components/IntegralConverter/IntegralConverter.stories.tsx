import React from 'react';
import IntegralConverter from './IntegralConverter';

export default {
  title: 'IntegralConverter',
  component: IntegralConverter,
  argTypes: {
  },
};

const Template = () => <IntegralConverter />;

export const withTemplate = Template.bind({});

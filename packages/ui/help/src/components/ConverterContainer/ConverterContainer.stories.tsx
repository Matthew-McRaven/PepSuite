import React from 'react';
import ConverterContainer from './ConverterContainer';

export default {
  title: 'ConverterContainer',
  component: ConverterContainer,
  argTypes: {
  },
};

const Template = () => <ConverterContainer />;

export const withTemplate = Template.bind({});
